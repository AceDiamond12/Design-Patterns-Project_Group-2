{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Maven: javafx:run",
      "type": "shell",
      "command": "mvn -q clean javafx:run",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "problemMatcher": []
    }
  ]
}